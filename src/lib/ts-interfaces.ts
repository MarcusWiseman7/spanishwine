export interface PopupObj {
    title: string;
    subtitle?: string;
    text?: string;
};

export interface Winery {
    name: string;
    url: string;
    image: {};
    location: {};
    description: string;
    wines: [],
    reviews: [];
};
